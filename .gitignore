# Python
__pycache__/
*.pyc
*.pyo
.venv/

# macOS
.DS_Store
__MACOSX/

# Claude Code local state (worktrees, plans, settings)
.claude/

# Generated artifacts — rebuild with: bash scripts/build.sh
# Excluded to keep repo size manageable for project knowledge sync
output/

# Generated PDF releases — distributed via GitHub Releases, not tracked in repo
releases/*.pdf

# Chat → Code staging area (transient content, consumed and deleted by Code)
staging/
